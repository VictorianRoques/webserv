Everyone:

- Chunk request what is it ? 
- Testeur du sujet

Victorian:

- Page de reponse Html + php
- Upload CGI

Padoue:

- ajout de la methode HEAD: si body dans request avec methode HEAD, ignorer requete
- gerer leak
- gerer new Location (remnove new)
- gerer excpetion server name (localhost:<port>)
- gerer path

Florian:

- Refaire Makefile
- tester sa mere le serveur , NO CRASH
- Recuperer liste des ports std::vector<size_t> _ports_g (belongs to Parser) dans sa fonction sockServ()
